{
  "version": 2,
  "builds": [
    {
      "src": "api/index.js", // Path to your Express application code
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "handle": "app" } // Redirect all requests to the "app" Serverless Function
  ]
}
